'use client'

interface Props {
  slot?: string
  format?: 'leaderboard' | 'rectangle' | 'responsive'
  className?: string
}

// TODO: replace with real AdSense code after approval
// e.g. <ins className="adsbygoogle" data-ad-client="ca-pub-XXXX" data-ad-slot={slot} .../>
export default function AdUnit(_props: Props) {
  return null
}
